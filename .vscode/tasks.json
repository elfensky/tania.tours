{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "activate SASS @ screen.scss",
            "type": "shell",
            "command": "sass --watch scss:css",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}